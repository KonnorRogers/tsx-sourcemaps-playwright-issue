{
  "type": "module",
  "scripts": {
    "test": "NODE_ENV=\"test\" NODE_OPTIONS='--enable-source-maps --import tsx' playwright test",
    "postinstall": "playwright install --with-deps"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "tsx": "^4.10.2"
  },
  "dependencies": {
    "@playwright/test": "^1.56.1"
  }
}
